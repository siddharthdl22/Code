public static int IsPalindrome(String s1, String s2)
	{
		int len1 = s1.length();
		int len2 = s2.length();
		int result = 1;
		
		for(int i = 0; i < len1; i++)
		{
			if(!(s1.charAt(i) == s2.charAt(len2 - i - 1)))
			{
				result = -1;
			}
		}
		return result;
	}
