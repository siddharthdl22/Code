class MyArray
{
  public void FindOnlyDuplicate(int[] arr)
  {
    int sum = 0;
    for(int i = 0; i < arr.length; i++)
    {
      sum = sum ^ arr[i];
    }
    System.out.println("The only duplicate element is : " + sum);
  }
}
