class MyArray
{
	public int FindMinimum(int[] arr)
	{
		int min = arr[0];
		for(int i = 1; i < arr.length; i++)
		{
			if(arr[i] < min)
				min = arr[i];
		}
		if(min > 0)
			return min--;
		return min++;
	}
}
